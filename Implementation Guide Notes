1. Branch Quality isn't needed just as we don't need confidence and we don't want to prune ever as we don't need to yet or anytime in the future this is a Local AGI not serving the whole world right like there is no need to prune either way we still traverse and chunk accordingly.

2. for ## 2.1 Add `resolve_overlap_llm` method it looks good but I feel like we aren't giving the prompt enough context on what its supposed to do so that it knows everytime like its not fully explaining the purpose and need of what we want also we will never fall back to fn rule_based_overlap_fallback because we should expect it to work or retry if not to pause gracefully and return errors so that a fix can be applied gracefully and to return to it from where paused

3. ## 3.1 Add detection methods this looks good as well but again I feel like we also aren't giving the prompt enough context IE we are dynamiclly loading in the modalities which is awesome but we don't provide a description per modality for it to truly understand what we are looking to capture and what formats etc based on each modality

4. for ## 4.1 Add grammar extraction method now again the same thing, this is expecting to obtain grammar extration and it's per chunk I believe and if so this means this would have multiple multiple and again with chunk reconstruction I feel like its best if we start where when processing chunks, we then start obtaining true text and identify all sentences and parapgrahs upon cleaning our chunks, so first we clean the chunks then identify all sentences and parapgrahs and then we can ID this IE identifying what sentence belongs in what parapgrahs as well as when we do chunk reconstruction so I say from here we do chunk reconstruction for all where we automatically then start reconstruction our chunks but actually I think its best not to do so here if we just convert everything to graphs because this is graphs as well we first start processing each chunk initially for a prompt/workspace/project so that we can deconstruct the chunks and convert them to graphs but we should be handling everything via graphs that is via AMT building the methodology detections

5. if state.root_modality_list.verified_modalities.is_empty() { this shouldn't be necessary because it should never be empty as we have it indexed and should be loading from that index dynamically

6. // STEP 3: Reconstruct cleaned prompt from chunks we don't need to reconstruct here as we can work with the chunks we have we just need to ensure that we now assign all the chunks to the promptID, this is important we save this this is historicall so we need to ensure also for promptID its unique and not the same as another

7. async fn classify_file_graphs_post_creation( is incorrect where called because it assumes keywords are intent which is incorrect this should be running running in the build_amt_layer_by_layer

8. 5.1 so the way we are handling create chunk graph it is expecting grammar releations to be passed in but again grammar relatinoships is to work on other graphs we need to review that create chunk graphs is actually now handling and storing all graphs accoringly with all nodes and edges becaues it seems now that it is just acting as if it was a dicitonary again this is in the create chunk graph it is expecting to parse senteces paragraphs etc here but this will be doing already in the process_chunks as we touched on in 4

9. taking into account 8 and 4 I have now obtained the clear ordering when we process chunks we first yes clean the chunk ensuring its gramatically correct, now extract_keywords,topics and entities should be handled in grammar_extration the reason why is because after we clean our chunk we then proceed to find paragraphs/senteces breaks etc and these are part of the graph itself of the text modality graph now when we say chunk graph I truly do not believe we need to have a seperate chunk graph as once we proceess a chunk once no matter what LLM context limit we really don't need to reconstruct it back to chunks as we are from here on out always working on a grpah based network we only chunk when processing once now back to this after we clean chunks we then identify all paragraphs and sentences in a chunk right and we here have nodes to our text modalit grapht root so we need to push as we find and establish our nodes connections here we still don't have edges yet now here is the thing after we obtain our sentences and parapgrahs we then per sentence do a grammar extraction and link this to each sentences then we build the depth by then doing a pairwise coparison across each sentinces and their nodes/edges of identified grammar this is comparing now sentnce per sentence 1x1 in a pairwise compariosn so here now we capture our full relationships graphs now I do believe after we capture all relationship graphs we then traverse all relationships found to identify keywords/topics/entities this gives more accurate results if we extract these by traverse our graph and built and identified relationships

10. We have to identify everywhere we do pairwise comparisons now here there is an optimization opportunity when it comes to zero-shotting based on context limit if we need to verify and stabalize we save work by comparison as many sets as possible at once and this dependson context limit so where we identify a pairwise coparison takes place we can also take into account batching or at least review to see if this is applicable to fit.

11. 5.3 validates mythoughts in 9 as chunk reconstruction at context limit is really unnecesary but we can keep if we ever come to find a purpose in the future maybe to rescan but if we rescan it would be just about maintain order so instead of chunk ID when we work with our content reviewing traversing we still also want to now keep order established so that we identify what came first and last and this even helps as well with change or update identification etc

12.  7.2 is found in our types modality


we still have to review each zero-shot prompt from the implementation guide to ensure completeness as these are drafts and not fully complete nor capturing all information with consistency
