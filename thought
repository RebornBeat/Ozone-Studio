The user is asking me to continue with the code modifications I was working on, covering all the different paths and integrating the insights from A through G, making sure the AMT graph path works properly with OMEX. I need to provide the before and after code blocks for both orchestrator/mod.rs and modalities/text/main.rs. B. Sentence extraction redesign:

Prompt too short, not specific enough
Don't provide full known sentence list — provide only the last worked sentence + count (O(1) state)
If list empty → instruct to provide FIRST sentence; else provide sentence FOLLOWING the given last sentence
Explain the role: correcting sentences, why we need it, how to return
5x validation redesign: NOT looping 5 times on the same extraction prompt. Extract ONCE, then run a separate validation prompt asking YES/NO: "is this sentence the sentence that follows this last sentence from this chunk" — 5 consecutive YES = validated, NO = rejected/re-extract. One-word JSON response.
Also 5x duplicate check against seen list (deterministic span guard first, LLM check as backstop)
Correction validation 5x YES/NO
Completion check: 5x NO to "is there any sentence after the last one not captured"
On validation: offload to SentenceNode immediately, keep only {order, original} in seen list
Cross-chunk sent

C. Section detection redesign:

Prompt must explain the state machine explicitly (IDLE, IN_SECTION, IN_SUBSECTION)
1x1 in order with scan offset
NO document boundary detection (removed from LLM prompting — left for graph traversal)
No formatting-break transitions — once section opens, stays open until same-or-higher marker or end of chunks
Keep subsection detection (lower level → nest, pop on return to higher level)
New enums: SectionTrackingState { Idle, InSection, InSubsection }, SectionEventType { SectionStart, SubsectionStart, SectionCloseAndStart }
Ancestor stack for nesting
5x YES/NO valid

For sentence carryover across chunks, I'm handling tail fragments beyond the last span as open fragments that prepend to the next chunk, and tracking token usage in the orchestrator state. For section detection, I'm implementing an explicit state machine that processes chunks sequentially with a scan offset, removing document boundary detection from the LLM and instead handling it during graph traversal. Sections stay open until encountering a same-or-higher level marker or reaching the end, with subsections nesting properly and popping when returning to higher levels, validated 5 times with YES/NO checks.
For modality detection, I'm dropping the "true_text" and "unknown" categories and processing modalities one at a time in order, using dual validation for both order and classification. Parent assignment ties to the sentence node if the modality is contained within one, otherwise it falls back to the nearest preceding sentence with a re-parent flag or the paragraph node. I'm also tracking open modalities that span chunk boundaries and including the parent node ID in the detection output.
For paragraph detection, I'm implementing two paths: the first uses prompt-based 1x1 detection with cross-chunk carryover for open paragraphs, while the second path is still being defined.
The overall processing flow splits into two approaches—a top-down deconstructor path that moves from sections to paragraphs to sentences with cleanup and modality detection, and a bottom-up constructor path that tracks section state while processing sentences and modalities per chunk, then constructs the paragraph and section hierarchy later in Phase 4 from the sentence graph.
For the constructor path, grammar extraction happens in Phase 3 before Phase 4 since those grammar edges are needed for the Stage 1 context pools. Cross-chunk aggregation items only apply to the deconstructor path, and basic metrics like word count and sentence count shift from text-based splits to graph-level aggregates.
I'm also considering adding a third execution mode for OMEX-native parsing, where a trained parser generates the entire chunk graph in a single pass. The orchestrator's AMT would then have a graph-traversal path that handles Stage 1 context accumulation, Stage 2 boundary evaluation, and Stage 3 promotion, while keeping the existing SLM and LLM paths functional through executor mode selection.
I need to clean up a few remaining issues: the rule-based structure extraction is already removed but DocumentStructure is still being built empty, metrics should derive directly from the graph (sentence count from SentenceNode count, paragraph count from ParagraphNode count), and grammar relationships are now tied to SentenceNode itself with grammar_nodes already in place. The full GrammarNodeType taxonomy and GrammarProperties are already defined in the code.
There are also some bugs to fix in the orchestrator module — get_available_pipelines is missing an else branch that should return an empty vector, and the stage labels and ordering are confused with build_amt being called before aggregate_root_modalities and create_initial_modality_graphs when it should come after.
In prompt_normalization, chunk.index is referenced in a debug statement but chunk isn't in scope there. And build_amt_layer_by_layer tries to mutate the orchestration state even though it only receives an immutable reference — it needs &mut OrchestrationState instead. There's also duplication in gather_methodologies that mirrors the AMT build step.
The control flow in build_amt_layer_by_layer is broken — there's an if block that ends with continue, so the tuple binding after it never completes and there's no else branch. Additionally, ModalityEvidence expects typed modality data but detected_modalities is just a vector of untyped JSON values, which causes issues when trying to access fields like detection.modality and detection.span_start in aggregate_root_modalities.
There's also a scope issue in modalities/text/main.rs where chunk_text references an undefined overlap_chars variable that isn't in the function signature.
The function signature mismatch — chunk_text is defined as an instance method but called as an associated function in analyze_text. Plus there's a type confusion mixing token counts with character indices when calculating the chunk end position.
In the DetectModalities action handler, the conditional for available_modalities is missing an else branch, and the ChunkModalityDetection constructor isn't setting the parent_node_id field that the struct now requires.
There are also several serde_json API issues: .as_usize() doesn't exist on Value objects (should be .as_u64().map(|x| x as usize)), and the generic syntax for HashMap in extract_grammar_from_graphs is malformed with broken line breaks in the type declaration.
When constructing Keyword structs, I need to compute is_phrase before moving the term field into the struct, since accessing term.contains(' ') after the move will fail. Additionally, TextGraphNode construction is missing the new universal fields like provisional and provenance — since the struct doesn't derive Default, I'll need to either add those fields explicitly or use ..Default::default() if I implement the trait. Path1 constructs ProcessedChunk with all required fields in place, so that's fine. Now I need to redesign detect_section_boundary and detect_paragraph_boundary per the requirements, plus rework the sentence extraction loop in process_chunk_path2. The analyze_text function computes basic metrics from the cleaned text.
There's a compilation issue: TextModalityAction::ChunkText doesn't exist in the enum, so the test won't compile. Also, the execute match is non-exhaustive—it's missing handlers for Normalize and possibly other actions like UpdateGraph, which will cause a compile error.
Additionally, extract_grammar_relationships_from_text is called in the execute method but isn't defined in the provided code. Based on the user's insight that grammar extraction should now be graph-based, this per-text method might need to be refactored or removed entirely. I need to clean up unused imports like regex::Regex since the rule-based extraction is gone, and I'm noticing a mismatch where chunk_text is defined as an async method taking &self but it's being called as a static method in analyze_text. I should also check if default_overlap_tokens() is still needed now that the chunking logic has changed. For the OMEX integration, I'm adding a third variant to the ProcessingPath enum called OmexNative that represents a single direct call to the OMEX parser, giving us three distinct paths: Path1 and Path2 for SLM/LLM approaches, and OmexNative as the dedicated OMEX execution route. I'm thinking through how to wire up the OMEX executor into the pipeline system—defining new pipeline IDs for the OMEX parser, realizer, and linker components, then having the text pipeline route to the OMEX executor instead of the LLM zero-shot calls when OMEX is enabled.
For the orchestrator's AMT implementation, I need to add a graph-based path that builds the AMT from the chunk graphs in three stages: accumulating context across sentence nodes and relationships, evaluating boundaries with evidence, then promoting to intents with aggregated knowledge references.
I'll use an enum to switch between the chunk-text zero-shot approach (Path 1) and the graph traversal approach (Path 2 / OMEX native), selected from the request config. The OMEX parser outputs knowledge spans on sentences, so I'm adding a KnowledgeRef struct and a corresponding field to SentenceNode to capture these references from the native path.
For grammar relationships, I need to add a grammar_relationships field to SentenceNode itself rather than keeping them at the chunk level — the user clarified this should be sentence-scoped. Phase 3 will extract subject/verb relationships per sentence.
On metrics, I'm switching from simple string-based counts to aggregating from the graph structure: sentence count becomes the total of all sentence nodes across chunks, paragraph count pulls from paragraph nodes (though it'll be zero for Path 2 until Phase 4), and word count comes from the corrected sentences. The user noted we don't need to calculate this ourselves since it comes from chunk processing.
For Phase 4 on Path 2, I need to implement construct_structure_from_graphs in main.rs to handle paragraph and section construction from the full corpus using graph-native evidence. This lives in the text modality layer and follows the flow chart's Phase 4 design, with Stage 1 pooling sentence order and grammar/coreference signals before Stage 2 evaluates boundaries and promotes to ParagraphNodes.
For Stage 1 boundary detection, I'm iterating through sentences in order and computing evidence for each adjacent pair: shared coreference chains, cross-sentence relationships, and overlapping entity texts from grammar subjects/objects. When evidence is ambiguous, I'll use zero-shot LLM evaluation for YES/NO boundary confirmation, keeping it deterministic and granular—one pair at a time, in sequence.
For the implementation, I'm refactoring the section boundary detection into a state machine approach with section tracking state, an ancestor stack to maintain hierarchy, and a scan offset loop that processes events one by one with dual validation passes. The paragraph boundary detection gets a similar treatment: a 1x1 loop that carries state across chunks (tracking whether a paragraph continues from the previous chunk) and validates boundaries with the same 5x YES/NO confirmation pattern.
For modalities, I'm switching to an ordered detection approach that classifies modalities and validates them, then ties them to parent sections using span containment. The sentence detection moves to a Path 2 implementation with helper functions that extract sentences one at a time using minimal state (just a count, last sentence, and offset), validate sentence ordering and corrections through dual 5x validation, and check whether all sentences in a chunk are complete.
I'm also adding a yes/no helper that can ask the model binary questions and confirm responses, plus carry states for sentences and modalities that thread through the analyze_text function alongside the existing section and paragraph states. Token accounting needs to track the response tokens we receive and add them to the used token count. I'll add an internal TokenMeter using an atomic counter to track LLM token usage within the pipeline, with helpers bumping it as they execute, then the orchestrator reads and resets it after each analysis stage. For the orchestrator itself, I need to fix the get_available_pipelines branch and reorder the execution stages to run input capture first, then file graphs, then text normalization.
After that comes the modality aggregation and graph building (which happens before AMT for Path2/Omex), followed by gathering methodologies without duplication, then selecting the AMT build strategy based on the path—either graph traversal for Path2/Omex or chunk-based for Path1—before moving into blueprint, simulation, and execution.
The real issue is that the stage numbering drifted from the intended eleven stages, and the function names still reference old stage numbers (like stage_3_* for blueprint), so I need to fix the execution order and remove the duplicated AMT build from gather_methodologies without renaming everything, since the user only wants changes to what they explicitly touched.
For the remaining fixes: I need to make build_amt_layer_by_layer properly handle mutability, restructure the intent tree-building loop to avoid the bogus tuple binding when there's only one intent capture, and remove the out-of-scope debug line about chunk.index since grammar relationships now live on individual SentenceNodes rather than at the aggregate result level.
The orchestrator's ProcessedChunk type needs to align with what the pipeline outputs — I should define a typed OrchModalityDetection struct with fields for modality, span boundaries, intent reference, chunk index, and parent node ID, then update ProcessedChunk.detected_modalities to use Vec<OrchModalityDetection> with serde defaults. I also notice the orchestrator's ProcessedChunk is missing sentence_nodes entirely, which needs to be addressed.
For the graph AMT construction, I need to add sentence-level data to the orchestrator: a slim OrchSentenceNode type containing node ID, content, original content, ordering info, paragraph and section IDs, plus lightweight copies of grammar relationships and knowledge references. I'll also include cross-sentence relationships and coreference chains with serde defaults so older payloads remain compatible. This is essential for the build_amt_from_graphs function to work properly in orchestrator mode.
Next I'm adding AmtBuildMode and ExecutorProfile types.
For routing, I'm introducing a ProcessingPathPref enum on OrchestrationRequest with variants for Auto, Path1, Path2, and OmexNative. The Auto variant will intelligently select the path based on model context: if the model identifier contains "omex" it uses OmexNative, if the context limit is 8192 or less it chooses Path2, otherwise it defaults to Path1.
Now for the graph-based context pooling, I'm building this in three stages: first, I construct context pools starting from each sentence and expanding through coreference chains, cross-sentence relationships, and shared grammar subjects/objects with normalized text matching using union-find or iterative merging. Then I evaluate boundaries between adjacent pools with evidence scoring, keeping pools below the linkage threshold separate. For ambiguous merges in the gray zone, I can optionally use zero-shot LLM confirmation to keep the model in the loop. Finally, each pool gets promoted to an IntentCapture structure capturing the actor and related details.
For promotion, I derive the actor, action, and target from the dominant grammar relationships in the pool — the most frequent subject becomes the actor, the verb becomes the action, and the object becomes the target. I can optionally condense this with a single zero-shot call per pool to produce a summary, though the docs suggest the evidence-based promotion alone is sufficient. Within each pool, I then identify sub-neighborhoods where sentences cluster around shared objects or topics, turning these into BranchCaptures with their source sentences and remaining leaf details. The key insight is reusing the existing capture-to-tree builder by factoring out the tree assembly logic into a shared assemble_amt_from_captures function, so both the layer-by-layer and graph-based modes can leverage the same tree construction without duplication.
For knowledge aggregation, pools accumulate knowledge references as a union and store them on the intent and branch metadata under a "knowledge_paths" field, while methodology IDs remain unchanged. I'm adding OMEX pipeline ID constants in the text main.rs and orchestrator — things like OMEX_TEXT_PARSER_PIPELINE_ID — and the OMEX parser invocation happens inside the text pipeline itself when processing chunks, with the orchestrator simply passing the processing_path through the Analyze action.
Now the orchestrator needs to include the processing_path in the Analyze input, and I'm extending the ProcessingPath enum in the text pipeline's Analyze action to support OmexNative with a serde alias. The OMEX-native processing flow calls the executor with the OMEX text parser, passing the chunk text, index, available modalities, and section state, then receives back parsed sentences with their order, original and corrected text, spans, and grammar tree information.
I'm mapping these parsed elements into the appropriate node types and performing deterministic verification checks on tree constraints like span containment and ordering — if verification fails for a chunk, I fall back to the alternative path. Since the user wants complete before-and-after code blocks for all modifications, I need to organize this systematically starting with the main.rs file changes.
First, I'm removing the unused regex import since extract_structure is gone, then adding the OMEX pipeline constants and extending the ProcessingPath enum to include the new OmexNative variant...
Adding enum variants and pipeline ID constants...
Now I'm defining the state and carry types that track sections, paragraphs, sentences, and modality information throughout the parsing process. I need to extend SentenceNode with grammar relationships and knowledge references, update ChunkModalityDetection to properly track parent nodes and section boundaries, and add LLM call metrics to TextModalityOutput.
I'm also reconsidering the enum variants — ExtractGrammarRelationships should be removed since grammar extraction is handled natively in Phase 3, and Normalize needs to go too since it's unhandled and the rule-based approach is being dropped. Both removals align with the design shift away from rule-based processing.
For metering token and call counts, I could add atomic counters to the Pipeline struct with helper methods, but that would require threading the calls through many executor sites. A cleaner approach is wrapping executor calls in a dedicated async method that handles both pipeline execution and metering in one place, replacing all the scattered self.executor.execute() calls with a single unified interface. Looking at the user's requirements, identify_documents contradicts the stated approach—they're deferring document tracking to future graph traversal and won't be tracking documents in the current phase. I should remove the identify_documents call from analyze_text and delete the function entirely, though the DocumentNode type stays for later use. I'll note this removal clearly in the refactoring.
Now for the chunk_text function, I'm converting it to a static method that uses character-based chunking with a fixed overlap strategy instead of token-based logic.  I need to be careful with UTF-8 boundaries when slicing — using char_indices to avoid panicking on invalid slice positions, and adding a helper to floor indices to safe character boundaries.
For analyze_text, I'm restructuring it to process chunks through a series of carry states: tracking sections first across all paths, then dispatching to path-specific logic like paragraph and sentence detection within each chunk.
For Path 1 specifically, I'm rethinking the flow: detect paragraphs one by one, then clean the whole chunk (keeping the zero-shot full-chunk approach from the original spec), and extract sentence boundaries within those paragraphs using a lightweight detector that the capable LLM can emit as a list. For Path 1, I'm creating a single LLM call that detects all sentences within a paragraph and returns them as an ordered list with byte spans and corrected text — this keeps the approach lightweight by trusting larger models to handle it in one shot rather than looping through sentences individually. Path 2 takes a different approach with sentence-by-sentence detection plus modality checks, skipping paragraph detection entirely.
Then I move into Phase 3 where I extract grammar relationships from the sentence graphs, building out the grammar_relationships field on each SentenceNode and fixing any compilation issues. Phase 4 (Path 2 only) reconstructs paragraphs from those graphs in two stages, then re-parents the modalities and ties everything back to sections. Finally, I aggregate metrics like word count from the corrected sentences, count the sentence and paragraph nodes, and meter those results into the output.
Now I'm setting up the new sentence extraction machinery to pull the next sentence, validate its ordering and check for duplicates or completion issues, then offload the work to SentenceNode and carry forward any fragments. I'm also building section tracking with a state machine that processes events one at a time and validates dual conditions, establishing parent-section relationships from the ancestor stack and carrying formatting patterns. The modality detection runs ordered validation across five checks with carry-through, then ties modalities to their parents. For Path 1 paragraphs, I'm detecting them within each chunk with fragment carry, while Path 2's paragraph construction pools evidence from coreference chains spanning sentences, cross-sentence relationship edges, and shared subjects before confirming boundaries.
Continuing with the paragraph assembly, I'm promoting ambiguous cases after five validation checks to ParagraphNodes, assigning sentence IDs and linking paragraphs back to their parent sections based on span coverage. Then I'm processing the OMEX chunk and verifying its output while parsing KnowledgeRef data. I need to fix compilation bugs in the grammar extraction from graphs and build grammar relationships onto sentence nodes, but there's a mismatch between how sentence IDs are formatted in the cross-sentence prompt versus how the relationship references use indices—I'm working on aligning those.
I also need to clean up unused loop variables and fix the TextGraphNode construction by creating helper functions that handle default field initialization, then replace all four construction sites with these helpers. Finally, I'm addressing a missing else clause in the DetectModalities handler and removing unnecessary text parameters.
For the action redesign, I'm updating DetectModalities to pass text, chunk_index, and available_modalities directly to the handler, which will call detect_modalities_ordered with a fresh carry state—this fixes the bug where chunk_index was hardcoded to 0. Then I'm removing the Normalize and ExtractGrammarRelationships variants entirely, along with identify_documents and any dead code like extract_strings_until_stable.
For overlap handling, I need to decide whether to keep resolve_overlap_llm and get_end_overlap. Since the new ordered extraction with fragment carry supersedes the old overlap resolution approach, get_end_overlap is unused and should go, but resolve_overlap_llm stays because Path 1 reconstruction still depends on it for boundary handling.
Now I'm cleaning up the dead code: removing extract_strings_until_stable, get_end_overlap, and several detection functions that were replaced by the new chunking logic, plus unused variants like Normalize and ExtractGrammarRelationships. The regex import can go too, though default_overlap_tokens stays since chunk_text now uses it.
For the main function and tests, I need to fix test_chunk_text since ChunkText action no longer exists—I'll convert it to a direct unit test of the chunk_text function instead. The test_analyze path still works with Path 1, but MockExecutor might need richer responses to handle the paragraph detection calls that Path 1 now makes.
The real issue is that when MockExecutor returns non-JSON or "test response", the JSON extraction fails, producing found:false, which means no paragraphs get detected. My design fallback treats the whole chunk as one implicit paragraph for sentence listing, but if that extraction also fails, I end up with zero sentence nodes and zero word_count, which breaks the assertion that word_count > 0. I need to add a fallback: if sentence extraction produces nothing, calculate word_count directly from the cleaned chunk text instead of relying on the graph. Now I'm setting up the orchestrator modifications to support the processing path preference. I need to add a new field to the OrchestrationRequest struct with serialization defaults, define an enum for the different path options, and create a resolution helper that determines which path to use based on the request, model, and context limits. Then I'm adding slim typed structs to ProcessedChunk that mirror the sentence and paragraph nodes with serde defaults for clean serialization. Updating the ProcessedChunk structure to include sentence nodes, cross-sentence relationships, and coreference chains with serde defaults, then removing the legacy grammar_relationships field since it's now captured in the sentence nodes. Then I need to fix the aggregate_root_modalities function to work with the new typed detections, address the get_available_pipelines issue, and reorder execute_stages to pass the processing path into Analyze while moving some initialization steps earlier in the pipeline.
For prompt_normalization, I'm passing the processing path and sizing max_chunk_tokens relative to the model's context window, collecting sentence data and token estimates from the LLM output, removing the broken grammar_relationships debug code, and implementing keyword derivation — when chunks lack keywords, I'll extract them from the sentence grammar relationships (subjects and objects) plus knowledge reference surfaces and topics.
For the AMT phase, I'm setting up an enum to choose between ChunkZeroShot and GraphTraversal modes based on whether any chunk contains sentence nodes with grammar relationships, then factoring out the tree assembly function to extract and fix the existing capture-building logic.
I'm also adding a new function that builds the AMT from graphs by first pooling sentences using union-find over coreference chains, cross-sentence relation edges, and shared normalized strings, then scoring evidence between adjacent pools and using zero-shot classification to resolve ambiguous cases by asking whether pairs of evidence pools describe the same goal. I need to sort out the stage numbering to avoid collisions—currently stage 5 is used both for consciousness gate and for AMT. I'm thinking through a cleaner mapping: stages 1–3 for Input, Text Normalization, and Initial Graph Creation, then fold Gather Methodologies into stage 3 rather than making it separate, which leaves room for AMT and consciousness gate without overlap. For token accounting, I'll add a helper function that meters LLM calls into the orchestration state after each execute step, capturing the prompt and result to track tokens used so far. This avoids borrow checker issues with trying to meter inline, and I'll apply it at the key call sites in the graph and AMT builder, plus the existing layer-by-layer processing points.
For tests, the MockExecutor pipeline needs to handle the new ProcessedChunk fields gracefully—since serde defaults are in place, missing fields like detected_modalities and grammar_relationships will default to empty, and the mock JSON can omit them without breaking compilation.
The test_basic_orchestration test constructs an OrchestrationRequest directly, so I need to add the processing_path field explicitly since Rust struct literals require all fields. I'll set it to ProcessingPathPref::Auto and include that in the modifications.
I'm going to structure the full answer with a brief intro, then walk through the text modality main.rs changes (T1 through Tn) with before/after blocks, followed by the orchestrator mod.rs changes (O1 through On), noting what I deliberately didn't change, and offering to continue if needed. Since this is a large scope, I'll write efficiently with full code blocks but minimal prose between them, prioritizing the text-modality rewrite first. I'm going to write out the text modality implementation, starting with updating the imports to remove regex and add atomic types for thread-safe counters, then defining the pipeline constant and working through the core structures and logic for handling text-based inputs. Defining state machine types for section tracking... Adding carry state fields... Adding knowledge reference structures... Defining the pipeline struct... Finishing the token estimation logic and then tracking the call metrics with atomic operations, then moving into a helper method that formats a yes/no question with specific JSON formatting requirements and temperature settings for deterministic responses. Adding validation methods... Writing chunk logic... Finishing up the output struct by swapping in the accumulated LLM call counts and token estimates. Now moving into the sentence identification machinery, which involves setting up prompt builders and the core loop that decomposes text chunks into ordered sentences for the graph structure. Building the prompt for sentence extraction... Implementing the sentence extraction loop with deterministic span validation to prevent overlaps, running the LLM checks five times for both order and correction validation, then storing the results in a seen list that tracks the original sentence and order.
When validation fails, I discard the candidate and retry extraction; if the same candidate fails twice, I escalate by recording a warning and treating the chunk as complete. I'm computing fragment carry by checking if there's trimmed non-empty text after the last span, then prepending it to the next chunk with adjusted offsets anchored to absolute positions. Section tracking happens before path dispatch so the current section stack is available for assignment, and modalities are ordered by sentence containment with parent tying to the nearest preceding sentence when needed.
For offset management, I'm storing node positions relative to the effective text but computing absolute prompt offsets by combining the fragment's absolute start with the local offset, or using the chunk start plus the adjusted local offset when the position falls outside the fragment. I'll implement a helper function to compute absolute offsets cleanly.
Now I'm setting up section tracking per chunk, which scans through the effective text and maintains section state across the document. The function processes section start and subsection start events, transitioning between idle and in-section states, and validates the ordering of these events.
I'm also working through the node type classification — the structural nodes like Section and Paragraph need to map to GrammarNodeType, but that enum currently only covers grammar-level constructs. I need to figure out whether to extend GrammarNodeType with structural variants or handle this differently. The user's suggestion to add those structural variants to the enum makes sense, so I'll add Paragraph, Section, Document under a structural banner and use them throughout. Now I'm mapping out the paragraph detection logic following the first path with carry-over handling, then setting up modality detection in the right order across all paths with dual validation checks and a helper function to tie modalities back to their parent nodes.
Writing the OMEX native path... Converting the parsed sentences and grammar tree into GrammarNode objects, verifying that spans are ordered correctly, non-overlapping, and contained within their parents, with each grammar tree rooted at a sentence. If analysis fails on a chunk, I'll fall back to an alternate path. Now I'm rewriting the analyze_text function to implement the full flow with metrics from the graph, then moving into Phase 3 to extract grammar from graphs—fixing bugs where per-sentence extraction adds both grammar_nodes and ChunkGrammarRelationship onto sentences, handling cross-sentence relationships by node_id, and properly managing coreference chains. For chunks that already carry grammar data, I'll skip redundant per-sentence extraction.
The key issue is that node_ids restart per chunk, causing collisions across the corpus. I need to assign globally unique ids in Phase 3 by renumbering all sentence node_ids sequentially before running the pairwise cross-sentence stage, and remapping any parent_node_id references that point to sentences.
Actually, a simpler approach: use a globally increasing id generator during per-chunk processing — I already have SentenceCarryState.next_global_order in path2, so I can extend that to generate unique sentence node_ids via Self::generate_id() (using nanosecond timestamps), which sections and paragraphs already do. This keeps ordering separate: chunk_id plus chunk_offset determines the sequence for sorting in prompts. The cross-sentence stage then uses these globally unique node_ids to map from_sentence_id and to_sentence_id in the response.
For coref chains, I'll store the entire chain on the chunk containing the first mention, avoiding duplication across chunks. The relationship itself lives on the chunk holding the from-sentence, and I need to implement the distribution logic accordingly.
Fixing create_graph next — adding base_node and base_edge helpers plus the keyword move fix, though the function stays structurally the same since TextAnalysisResult now has an empty sections structure and the orchestrator still calls it via CreateGraph with the analysis result.
For basic metrics in analyze_text, I'm computing sentence and paragraph counts directly from the graph nodes after cleaning, then deriving word count from the joined corrected sentence content rather than splitting the cleaned text again.
Now I'm refactoring the DetectModalities handler with a new signature and compile fixes, then removing several helper functions like identify_documents, detect_section_boundary, and the various detect_modalities variants since they're no longer needed. The main stdin stub stays unchanged, but I'm replacing the chunk text test with a synchronous unit test that mocks the cleaning prompt to return "Cleaned text here." and verifies the fallback behavior when JSON extraction fails. Mapping out the orchestrator refactoring — I need to handle the request/preference enum and resolver, update the ProcessedChunk types, fix get_available_pipelines, replace execute_stages and prompt_normalization with cleaner versions, trim gather_methodologies, and then tackle the big piece: the AMT builder with its dispatcher, assembly logic, layer-by-layer construction, and intent-tree bug fixes.
Building build_amt_from_graphs... Still writing union-find logic... Wrapping each pool into an IntentCapture with metadata about parallelism and source tracking, then grouping sentences within pools by their target object or knowledge topic to create branches with summarized verbs, and finally extracting detail-level grammar relationships where negations or modal verbs flag constraints versus regular details. I'm setting up the knowledge paths mapping to track which branches have associated knowledge, then passing that through to the assembly function as an optional parameter that stays empty in zero-shot mode. The assembly function signature now accepts this knowledge map along with the orchestration state and node counter.
Now I'm structuring the tree construction to handle multiple intents by checking if there's more than one capture—if so, each intent gets its own subtree with branches nested underneath, otherwise branches attach directly to the root. I'm factoring out the branch-building logic into a reusable method to avoid duplication across these two cases.
The main orchestration dispatcher checks whether to build the tree from existing file graphs or layer-by-layer based on what's been processed, then validates the result and tracks the validation streak.
For the gather_methodologies step, I'm simplifying it to just record the summary without repeating the classify-build-validate cycle. Then I need to update prompt_normalization to compute the processing path and token budget, passing those into the process_modality function so it can construct the Analyze action with the right context parameters.
I'm also tracking token usage from the LLM response and accumulating it in the state. When keywords haven't been derived yet, I'm traversing the processed chunks to extract subject and object relationships from the grammar nodes.
Now I'm implementing the path resolution function that maps the processing preference to its string representation, with special handling for the Auto variant that checks the model configuration.
For the context limit mapping, I'm considering whether OMEX models should get a higher default since they handle chunking differently than standard context-window-bound models.
I'm restructuring the execute_stages function to follow the correct order: input processing, file handling within stage 2, prompt normalization, and then the modality graph creation steps.
The clarification check needs to stay after the AMT build phase since that's where needs_clarification gets set during simulation, though it could be populated by AMT in the future.
I'm also updating the aggregate_root_modalities function to use the typed OrchModalityDetection, and I need to add the O2 struct block before the ProcessedChunk definitions.
For the test fix, I'll add processing_path to the request literal, and I should check if there are any other references to the grammar_relationships field that was removed from ProcessedChunk in the orchestrator.
Looking at the pipeline output, it now includes sentence_nodes with grammar_relationships and knowledge_refs, so the orchestrator's slim structs need to match those field names exactly — node_id, content, original_content, chunk_id, chunk_offset, paragraph_id, section_id, and the grammar_relationships fields like from_text, to_text, edge_type, tense, negated, verb.
For the verb_type enum that the pipeline serializes as a string like "Action", I'm mapping it to a String in the slim struct with serde defaults to handle optional fields, and since serde ignores unknown fields, I can skip position and nested fields that aren't needed for the pools. The knowledge_refs and cross_sentence_relationships fields mirror the pipeline structure with from_sentence_id, to_sentence_id, relationship_type, and evidence, plus coreference chains with chain_id.
Now I'm ready to write the full implementation — I'll structure it as a short intro paragraph followed by Part 1 covering the modalities/text/main.rs modules with numbered sections and minimal commentary, then Part 2 for the orchestrator/mod.rs. I'm aiming for completeness and will be careful about Rust correctness throughout.
Writing the Rust implementation... So the flow is: detect paragraphs and sentences on the raw text first to anchor spans correctly, then apply whole-chunk cleaning for the corrected text used downstream in keyword/entity/topic extraction, while keeping sentence nodes anchored to the original text for proper byte positioning. For modality and parent relationships in Path1, I'm tying sentences to paragraphs by span containment, then falling back to paragraph containment if needed. Path1 doesn't need the five validation loops that Path2 uses—just a single extraction with deterministic monotonic ordering to keep things consistent. Section tracking continues for the next component. Path1 paragraphs stay prompt-driven without granular validation, keeping it simple. For modality detection in step 3, I'll apply the dual 5x validation universally across both paths. On the state management side, I'm dropping the global order tracking since order restarts per chunk anyway — I'll simplify SentenceCarryState to just track fragment and its absolute position.
Now I'm ready to implement. I'll number the text modifications T1–T24 and orchestrator changes O1–O12, keeping each one concise. For the code structure, I need to make sure GrammarNodeType derives Default properly with the Unknown variant, and map each node type correctly — Section, Paragraph, and Document nodes all get their corresponding enum values.
ChunkGrammarRelationship will use a String for verb_type instead of the full VerbType enum in the orchestrator version. TextModalityOutput needs its Default implementation updated to include the two new fields. The detect_modalities_ordered function takes the chunk text, index, available modalities list, and a mutable carry state, then returns a vector of detections. When carry.open is true, the first prompt targets the end of that open block and produces a detection spanning from 0 to the end with the carried modality.
For continuation handling, I'm storing the local span start at 0 and letting the caller manage absolute positioning, then marking the detection with an intent_reference noting it continues from a previous chunk. I'm extracting the modality detection prompt per the earlier draft and running two validation checks: first confirming whether the span at the given boundaries is the first modality occurrence after a specific offset, then classifying by including the actual text slice. For safety when slicing spans returned by the model, I'm clamping boundaries and using character boundary flooring on both ends.
Writing helper function... I'm realizing the scope here is substantial—fitting all the before/after code examples in a single response will likely exceed the length limit, so I'll need to strategically prioritize and mark continuation points as needed.

The user is asking me to continue with the code modifications I was working on, covering all the different paths and integrating the insights from A through G, making sure the AMT graph path works properly with OMEX. I need to provide the before and after code blocks for both orchestrator/mod.rs and modalities/text/main.rs. B. Sentence extraction redesign:

Prompt too short, not specific enough
Don't provide full known sentence list — provide only the last worked sentence + count (O(1) state)
If list empty → instruct to provide FIRST sentence; else provide sentence FOLLOWING the given last sentence
Explain the role: correcting sentences, why we need it, how to return
5x validation redesign: NOT looping 5 times on the same extraction prompt. Extract ONCE, then run a separate validation prompt asking YES/NO: "is this sentence the sentence that follows this last sentence from this chunk" — 5 consecutive YES = validated, NO = rejected/re-extract. One-word JSON response.
Also 5x duplicate check against seen list (deterministic span guard first, LLM check as backstop)
Correction validation 5x YES/NO
Completion check: 5x NO to "is there any sentence after the last one not captured"
On validation: offload to SentenceNode immediately, keep only {order, original} in seen list
Cross-chunk sent

C. Section detection redesign:

Prompt must explain the state machine explicitly (IDLE, IN_SECTION, IN_SUBSECTION)
1x1 in order with scan offset
NO document boundary detection (removed from LLM prompting — left for graph traversal)
No formatting-break transitions — once section opens, stays open until same-or-higher marker or end of chunks
Keep subsection detection (lower level → nest, pop on return to higher level)
New enums: SectionTrackingState { Idle, InSection, InSubsection }, SectionEventType { SectionStart, SubsectionStart, SectionCloseAndStart }
Ancestor stack for nesting
5x YES/NO valid

For sentence carryover across chunks, I'm handling tail fragments beyond the last span as open fragments that prepend to the next chunk, and tracking token usage in the orchestrator state. For section detection, I'm implementing an explicit state machine that processes chunks sequentially with a scan offset, removing document boundary detection from the LLM and instead handling it during graph traversal. Sections stay open until encountering a same-or-higher level marker or reaching the end, with subsections nesting properly and popping when returning to higher levels, validated 5 times with YES/NO checks.
For modality detection, I'm dropping the "true_text" and "unknown" categories and processing modalities one at a time in order, using dual validation for both order and classification. Parent assignment ties to the sentence node if the modality is contained within one, otherwise it falls back to the nearest preceding sentence with a re-parent flag or the paragraph node. I'm also tracking open modalities that span chunk boundaries and including the parent node ID in the detection output.
For paragraph detection, I'm implementing two paths: the first uses prompt-based 1x1 detection with cross-chunk carryover for open paragraphs, while the second path is still being defined.
The overall processing flow splits into two approaches—a top-down deconstructor path that moves from sections to paragraphs to sentences with cleanup and modality detection, and a bottom-up constructor path that tracks section state while processing sentences and modalities per chunk, then constructs the paragraph and section hierarchy later in Phase 4 from the sentence graph.
For the constructor path, grammar extraction happens in Phase 3 before Phase 4 since those grammar edges are needed for the Stage 1 context pools. Cross-chunk aggregation items only apply to the deconstructor path, and basic metrics like word count and sentence count shift from text-based splits to graph-level aggregates.
I'm also considering adding a third execution mode for OMEX-native parsing, where a trained parser generates the entire chunk graph in a single pass. The orchestrator's AMT would then have a graph-traversal path that handles Stage 1 context accumulation, Stage 2 boundary evaluation, and Stage 3 promotion, while keeping the existing SLM and LLM paths functional through executor mode selection.
I need to clean up a few remaining issues: the rule-based structure extraction is already removed but DocumentStructure is still being built empty, metrics should derive directly from the graph (sentence count from SentenceNode count, paragraph count from ParagraphNode count), and grammar relationships are now tied to SentenceNode itself with grammar_nodes already in place. The full GrammarNodeType taxonomy and GrammarProperties are already defined in the code.
There are also some bugs to fix in the orchestrator module — get_available_pipelines is missing an else branch that should return an empty vector, and the stage labels and ordering are confused with build_amt being called before aggregate_root_modalities and create_initial_modality_graphs when it should come after.
In prompt_normalization, chunk.index is referenced in a debug statement but chunk isn't in scope there. And build_amt_layer_by_layer tries to mutate the orchestration state even though it only receives an immutable reference — it needs &mut OrchestrationState instead. There's also duplication in gather_methodologies that mirrors the AMT build step.
The control flow in build_amt_layer_by_layer is broken — there's an if block that ends with continue, so the tuple binding after it never completes and there's no else branch. Additionally, ModalityEvidence expects typed modality data but detected_modalities is just a vector of untyped JSON values, which causes issues when trying to access fields like detection.modality and detection.span_start in aggregate_root_modalities.
There's also a scope issue in modalities/text/main.rs where chunk_text references an undefined overlap_chars variable that isn't in the function signature.
The function signature mismatch — chunk_text is defined as an instance method but called as an associated function in analyze_text. Plus there's a type confusion mixing token counts with character indices when calculating the chunk end position.
In the DetectModalities action handler, the conditional for available_modalities is missing an else branch, and the ChunkModalityDetection constructor isn't setting the parent_node_id field that the struct now requires.
There are also several serde_json API issues: .as_usize() doesn't exist on Value objects (should be .as_u64().map(|x| x as usize)), and the generic syntax for HashMap in extract_grammar_from_graphs is malformed with broken line breaks in the type declaration.
When constructing Keyword structs, I need to compute is_phrase before moving the term field into the struct, since accessing term.contains(' ') after the move will fail. Additionally, TextGraphNode construction is missing the new universal fields like provisional and provenance — since the struct doesn't derive Default, I'll need to either add those fields explicitly or use ..Default::default() if I implement the trait. Path1 constructs ProcessedChunk with all required fields in place, so that's fine. Now I need to redesign detect_section_boundary and detect_paragraph_boundary per the requirements, plus rework the sentence extraction loop in process_chunk_path2. The analyze_text function computes basic metrics from the cleaned text.
There's a compilation issue: TextModalityAction::ChunkText doesn't exist in the enum, so the test won't compile. Also, the execute match is non-exhaustive—it's missing handlers for Normalize and possibly other actions like UpdateGraph, which will cause a compile error.
Additionally, extract_grammar_relationships_from_text is called in the execute method but isn't defined in the provided code. Based on the user's insight that grammar extraction should now be graph-based, this per-text method might need to be refactored or removed entirely. I need to clean up unused imports like regex::Regex since the rule-based extraction is gone, and I'm noticing a mismatch where chunk_text is defined as an async method taking &self but it's being called as a static method in analyze_text. I should also check if default_overlap_tokens() is still needed now that the chunking logic has changed. For the OMEX integration, I'm adding a third variant to the ProcessingPath enum called OmexNative that represents a single direct call to the OMEX parser, giving us three distinct paths: Path1 and Path2 for SLM/LLM approaches, and OmexNative as the dedicated OMEX execution route. I'm thinking through how to wire up the OMEX executor into the pipeline system—defining new pipeline IDs for the OMEX parser, realizer, and linker components, then having the text pipeline route to the OMEX executor instead of the LLM zero-shot calls when OMEX is enabled.
For the orchestrator's AMT implementation, I need to add a graph-based path that builds the AMT from the chunk graphs in three stages: accumulating context across sentence nodes and relationships, evaluating boundaries with evidence, then promoting to intents with aggregated knowledge references.
I'll use an enum to switch between the chunk-text zero-shot approach (Path 1) and the graph traversal approach (Path 2 / OMEX native), selected from the request config. The OMEX parser outputs knowledge spans on sentences, so I'm adding a KnowledgeRef struct and a corresponding field to SentenceNode to capture these references from the native path.
For grammar relationships, I need to add a grammar_relationships field to SentenceNode itself rather than keeping them at the chunk level — the user clarified this should be sentence-scoped. Phase 3 will extract subject/verb relationships per sentence.
On metrics, I'm switching from simple string-based counts to aggregating from the graph structure: sentence count becomes the total of all sentence nodes across chunks, paragraph count pulls from paragraph nodes (though it'll be zero for Path 2 until Phase 4), and word count comes from the corrected sentences. The user noted we don't need to calculate this ourselves since it comes from chunk processing.
For Phase 4 on Path 2, I need to implement construct_structure_from_graphs in main.rs to handle paragraph and section construction from the full corpus using graph-native evidence. This lives in the text modality layer and follows the flow chart's Phase 4 design, with Stage 1 pooling sentence order and grammar/coreference signals before Stage 2 evaluates boundaries and promotes to ParagraphNodes.
For Stage 1 boundary detection, I'm iterating through sentences in order and computing evidence for each adjacent pair: shared coreference chains, cross-sentence relationships, and overlapping entity texts from grammar subjects/objects. When evidence is ambiguous, I'll use zero-shot LLM evaluation for YES/NO boundary confirmation, keeping it deterministic and granular—one pair at a time, in sequence.
For the implementation, I'm refactoring the section boundary detection into a state machine approach with section tracking state, an ancestor stack to maintain hierarchy, and a scan offset loop that processes events one by one with dual validation passes. The paragraph boundary detection gets a similar treatment: a 1x1 loop that carries state across chunks (tracking whether a paragraph continues from the previous chunk) and validates boundaries with the same 5x YES/NO confirmation pattern.
For modalities, I'm switching to an ordered detection approach that classifies modalities and validates them, then ties them to parent sections using span containment. The sentence detection moves to a Path 2 implementation with helper functions that extract sentences one at a time using minimal state (just a count, last sentence, and offset), validate sentence ordering and corrections through dual 5x validation, and check whether all sentences in a chunk are complete.
I'm also adding a yes/no helper that can ask the model binary questions and confirm responses, plus carry states for sentences and modalities that thread through the analyze_text function alongside the existing section and paragraph states. Token accounting needs to track the response tokens we receive and add them to the used token count. I'll add an internal TokenMeter using an atomic counter to track LLM token usage within the pipeline, with helpers bumping it as they execute, then the orchestrator reads and resets it after each analysis stage. For the orchestrator itself, I need to fix the get_available_pipelines branch and reorder the execution stages to run input capture first, then file graphs, then text normalization.
After that comes the modality aggregation and graph building (which happens before AMT for Path2/Omex), followed by gathering methodologies without duplication, then selecting the AMT build strategy based on the path—either graph traversal for Path2/Omex or chunk-based for Path1—before moving into blueprint, simulation, and execution.
The real issue is that the stage numbering drifted from the intended eleven stages, and the function names still reference old stage numbers (like stage_3_* for blueprint), so I need to fix the execution order and remove the duplicated AMT build from gather_methodologies without renaming everything, since the user only wants changes to what they explicitly touched.
For the remaining fixes: I need to make build_amt_layer_by_layer properly handle mutability, restructure the intent tree-building loop to avoid the bogus tuple binding when there's only one intent capture, and remove the out-of-scope debug line about chunk.index since grammar relationships now live on individual SentenceNodes rather than at the aggregate result level.
The orchestrator's ProcessedChunk type needs to align with what the pipeline outputs — I should define a typed OrchModalityDetection struct with fields for modality, span boundaries, intent reference, chunk index, and parent node ID, then update ProcessedChunk.detected_modalities to use Vec<OrchModalityDetection> with serde defaults. I also notice the orchestrator's ProcessedChunk is missing sentence_nodes entirely, which needs to be addressed.
For the graph AMT construction, I need to add sentence-level data to the orchestrator: a slim OrchSentenceNode type containing node ID, content, original content, ordering info, paragraph and section IDs, plus lightweight copies of grammar relationships and knowledge references. I'll also include cross-sentence relationships and coreference chains with serde defaults so older payloads remain compatible. This is essential for the build_amt_from_graphs function to work properly in orchestrator mode.
Next I'm adding AmtBuildMode and ExecutorProfile types.
For routing, I'm introducing a ProcessingPathPref enum on OrchestrationRequest with variants for Auto, Path1, Path2, and OmexNative. The Auto variant will intelligently select the path based on model context: if the model identifier contains "omex" it uses OmexNative, if the context limit is 8192 or less it chooses Path2, otherwise it defaults to Path1.
Now for the graph-based context pooling, I'm building this in three stages: first, I construct context pools starting from each sentence and expanding through coreference chains, cross-sentence relationships, and shared grammar subjects/objects with normalized text matching using union-find or iterative merging. Then I evaluate boundaries between adjacent pools with evidence scoring, keeping pools below the linkage threshold separate. For ambiguous merges in the gray zone, I can optionally use zero-shot LLM confirmation to keep the model in the loop. Finally, each pool gets promoted to an IntentCapture structure capturing the actor and related details.
For promotion, I derive the actor, action, and target from the dominant grammar relationships in the pool — the most frequent subject becomes the actor, the verb becomes the action, and the object becomes the target. I can optionally condense this with a single zero-shot call per pool to produce a summary, though the docs suggest the evidence-based promotion alone is sufficient. Within each pool, I then identify sub-neighborhoods where sentences cluster around shared objects or topics, turning these into BranchCaptures with their source sentences and remaining leaf details. The key insight is reusing the existing capture-to-tree builder by factoring out the tree assembly logic into a shared assemble_amt_from_captures function, so both the layer-by-layer and graph-based modes can leverage the same tree construction without duplication.
For knowledge aggregation, pools accumulate knowledge references as a union and store them on the intent and branch metadata under a "knowledge_paths" field, while methodology IDs remain unchanged. I'm adding OMEX pipeline ID constants in the text main.rs and orchestrator — things like OMEX_TEXT_PARSER_PIPELINE_ID — and the OMEX parser invocation happens inside the text pipeline itself when processing chunks, with the orchestrator simply passing the processing_path through the Analyze action.
Now the orchestrator needs to include the processing_path in the Analyze input, and I'm extending the ProcessingPath enum in the text pipeline's Analyze action to support OmexNative with a serde alias. The OMEX-native processing flow calls the executor with the OMEX text parser, passing the chunk text, index, available modalities, and section state, then receives back parsed sentences with their order, original and corrected text, spans, and grammar tree information.
I'm mapping these parsed elements into the appropriate node types and performing deterministic verification checks on tree constraints like span containment and ordering — if verification fails for a chunk, I fall back to the alternative path. Since the user wants complete before-and-after code blocks for all modifications, I need to organize this systematically starting with the main.rs file changes.
First, I'm removing the unused regex import since extract_structure is gone, then adding the OMEX pipeline constants and extending the ProcessingPath enum to include the new OmexNative variant...
Adding enum variants and pipeline ID constants...
Now I'm defining the state and carry types that track sections, paragraphs, sentences, and modality information throughout the parsing process. I need to extend SentenceNode with grammar relationships and knowledge references, update ChunkModalityDetection to properly track parent nodes and section boundaries, and add LLM call metrics to TextModalityOutput.
I'm also reconsidering the enum variants — ExtractGrammarRelationships should be removed since grammar extraction is handled natively in Phase 3, and Normalize needs to go too since it's unhandled and the rule-based approach is being dropped. Both removals align with the design shift away from rule-based processing.
For metering token and call counts, I could add atomic counters to the Pipeline struct with helper methods, but that would require threading the calls through many executor sites. A cleaner approach is wrapping executor calls in a dedicated async method that handles both pipeline execution and metering in one place, replacing all the scattered self.executor.execute() calls with a single unified interface. Looking at the user's requirements, identify_documents contradicts the stated approach—they're deferring document tracking to future graph traversal and won't be tracking documents in the current phase. I should remove the identify_documents call from analyze_text and delete the function entirely, though the DocumentNode type stays for later use. I'll note this removal clearly in the refactoring.
Now for the chunk_text function, I'm converting it to a static method that uses character-based chunking with a fixed overlap strategy instead of token-based logic.  I need to be careful with UTF-8 boundaries when slicing — using char_indices to avoid panicking on invalid slice positions, and adding a helper to floor indices to safe character boundaries.
For analyze_text, I'm restructuring it to process chunks through a series of carry states: tracking sections first across all paths, then dispatching to path-specific logic like paragraph and sentence detection within each chunk.
For Path 1 specifically, I'm rethinking the flow: detect paragraphs one by one, then clean the whole chunk (keeping the zero-shot full-chunk approach from the original spec), and extract sentence boundaries within those paragraphs using a lightweight detector that the capable LLM can emit as a list. For Path 1, I'm creating a single LLM call that detects all sentences within a paragraph and returns them as an ordered list with byte spans and corrected text — this keeps the approach lightweight by trusting larger models to handle it in one shot rather than looping through sentences individually. Path 2 takes a different approach with sentence-by-sentence detection plus modality checks, skipping paragraph detection entirely.
Then I move into Phase 3 where I extract grammar relationships from the sentence graphs, building out the grammar_relationships field on each SentenceNode and fixing any compilation issues. Phase 4 (Path 2 only) reconstructs paragraphs from those graphs in two stages, then re-parents the modalities and ties everything back to sections. Finally, I aggregate metrics like word count from the corrected sentences, count the sentence and paragraph nodes, and meter those results into the output.
Now I'm setting up the new sentence extraction machinery to pull the next sentence, validate its ordering and check for duplicates or completion issues, then offload the work to SentenceNode and carry forward any fragments. I'm also building section tracking with a state machine that processes events one at a time and validates dual conditions, establishing parent-section relationships from the ancestor stack and carrying formatting patterns. The modality detection runs ordered validation across five checks with carry-through, then ties modalities to their parents. For Path 1 paragraphs, I'm detecting them within each chunk with fragment carry, while Path 2's paragraph construction pools evidence from coreference chains spanning sentences, cross-sentence relationship edges, and shared subjects before confirming boundaries.
Continuing with the paragraph assembly, I'm promoting ambiguous cases after five validation checks to ParagraphNodes, assigning sentence IDs and linking paragraphs back to their parent sections based on span coverage. Then I'm processing the OMEX chunk and verifying its output while parsing KnowledgeRef data. I need to fix compilation bugs in the grammar extraction from graphs and build grammar relationships onto sentence nodes, but there's a mismatch between how sentence IDs are formatted in the cross-sentence prompt versus how the relationship references use indices—I'm working on aligning those.
I also need to clean up unused loop variables and fix the TextGraphNode construction by creating helper functions that handle default field initialization, then replace all four construction sites with these helpers. Finally, I'm addressing a missing else clause in the DetectModalities handler and removing unnecessary text parameters.
For the action redesign, I'm updating DetectModalities to pass text, chunk_index, and available_modalities directly to the handler, which will call detect_modalities_ordered with a fresh carry state—this fixes the bug where chunk_index was hardcoded to 0. Then I'm removing the Normalize and ExtractGrammarRelationships variants entirely, along with identify_documents and any dead code like extract_strings_until_stable.
For overlap handling, I need to decide whether to keep resolve_overlap_llm and get_end_overlap. Since the new ordered extraction with fragment carry supersedes the old overlap resolution approach, get_end_overlap is unused and should go, but resolve_overlap_llm stays because Path 1 reconstruction still depends on it for boundary handling.
Now I'm cleaning up the dead code: removing extract_strings_until_stable, get_end_overlap, and several detection functions that were replaced by the new chunking logic, plus unused variants like Normalize and ExtractGrammarRelationships. The regex import can go too, though default_overlap_tokens stays since chunk_text now uses it.
For the main function and tests, I need to fix test_chunk_text since ChunkText action no longer exists—I'll convert it to a direct unit test of the chunk_text function instead. The test_analyze path still works with Path 1, but MockExecutor might need richer responses to handle the paragraph detection calls that Path 1 now makes.
The real issue is that when MockExecutor returns non-JSON or "test response", the JSON extraction fails, producing found:false, which means no paragraphs get detected. My design fallback treats the whole chunk as one implicit paragraph for sentence listing, but if that extraction also fails, I end up with zero sentence nodes and zero word_count, which breaks the assertion that word_count > 0. I need to add a fallback: if sentence extraction produces nothing, calculate word_count directly from the cleaned chunk text instead of relying on the graph. Now I'm setting up the orchestrator modifications to support the processing path preference. I need to add a new field to the OrchestrationRequest struct with serialization defaults, define an enum for the different path options, and create a resolution helper that determines which path to use based on the request, model, and context limits. Then I'm adding slim typed structs to ProcessedChunk that mirror the sentence and paragraph nodes with serde defaults for clean serialization. Updating the ProcessedChunk structure to include sentence nodes, cross-sentence relationships, and coreference chains with serde defaults, then removing the legacy grammar_relationships field since it's now captured in the sentence nodes. Then I need to fix the aggregate_root_modalities function to work with the new typed detections, address the get_available_pipelines issue, and reorder execute_stages to pass the processing path into Analyze while moving some initialization steps earlier in the pipeline.
For prompt_normalization, I'm passing the processing path and sizing max_chunk_tokens relative to the model's context window, collecting sentence data and token estimates from the LLM output, removing the broken grammar_relationships debug code, and implementing keyword derivation — when chunks lack keywords, I'll extract them from the sentence grammar relationships (subjects and objects) plus knowledge reference surfaces and topics.
For the AMT phase, I'm setting up an enum to choose between ChunkZeroShot and GraphTraversal modes based on whether any chunk contains sentence nodes with grammar relationships, then factoring out the tree assembly function to extract and fix the existing capture-building logic.
I'm also adding a new function that builds the AMT from graphs by first pooling sentences using union-find over coreference chains, cross-sentence relation edges, and shared normalized strings, then scoring evidence between adjacent pools and using zero-shot classification to resolve ambiguous cases by asking whether pairs of evidence pools describe the same goal. I need to sort out the stage numbering to avoid collisions—currently stage 5 is used both for consciousness gate and for AMT. I'm thinking through a cleaner mapping: stages 1–3 for Input, Text Normalization, and Initial Graph Creation, then fold Gather Methodologies into stage 3 rather than making it separate, which leaves room for AMT and consciousness gate without overlap. For token accounting, I'll add a helper function that meters LLM calls into the orchestration state after each execute step, capturing the prompt and result to track tokens used so far. This avoids borrow checker issues with trying to meter inline, and I'll apply it at the key call sites in the graph and AMT builder, plus the existing layer-by-layer processing points.
For tests, the MockExecutor pipeline needs to handle the new ProcessedChunk fields gracefully—since serde defaults are in place, missing fields like detected_modalities and grammar_relationships will default to empty, and the mock JSON can omit them without breaking compilation.
The test_basic_orchestration test constructs an OrchestrationRequest directly, so I need to add the processing_path field explicitly since Rust struct literals require all fields. I'll set it to ProcessingPathPref::Auto and include that in the modifications.
I'm going to structure the full answer with a brief intro, then walk through the text modality main.rs changes (T1 through Tn) with before/after blocks, followed by the orchestrator mod.rs changes (O1 through On), noting what I deliberately didn't change, and offering to continue if needed. Since this is a large scope, I'll write efficiently with full code blocks but minimal prose between them, prioritizing the text-modality rewrite first. I'm going to write out the text modality implementation, starting with updating the imports to remove regex and add atomic types for thread-safe counters, then defining the pipeline constant and working through the core structures and logic for handling text-based inputs. Defining state machine types for section tracking... Adding carry state fields... Adding knowledge reference structures... Defining the pipeline struct... Finishing the token estimation logic and then tracking the call metrics with atomic operations, then moving into a helper method that formats a yes/no question with specific JSON formatting requirements and temperature settings for deterministic responses. Adding validation methods... Writing chunk logic... Finishing up the output struct by swapping in the accumulated LLM call counts and token estimates. Now moving into the sentence identification machinery, which involves setting up prompt builders and the core loop that decomposes text chunks into ordered sentences for the graph structure. Building the prompt for sentence extraction... Implementing the sentence extraction loop with deterministic span validation to prevent overlaps, running the LLM checks five times for both order and correction validation, then storing the results in a seen list that tracks the original sentence and order.
When validation fails, I discard the candidate and retry extraction; if the same candidate fails twice, I escalate by recording a warning and treating the chunk as complete. I'm computing fragment carry by checking if there's trimmed non-empty text after the last span, then prepending it to the next chunk with adjusted offsets anchored to absolute positions. Section tracking happens before path dispatch so the current section stack is available for assignment, and modalities are ordered by sentence containment with parent tying to the nearest preceding sentence when needed.
For offset management, I'm storing node positions relative to the effective text but computing absolute prompt offsets by combining the fragment's absolute start with the local offset, or using the chunk start plus the adjusted local offset when the position falls outside the fragment. I'll implement a helper function to compute absolute offsets cleanly.
Now I'm setting up section tracking per chunk, which scans through the effective text and maintains section state across the document. The function processes section start and subsection start events, transitioning between idle and in-section states, and validates the ordering of these events.
I'm also working through the node type classification — the structural nodes like Section and Paragraph need to map to GrammarNodeType, but that enum currently only covers grammar-level constructs. I need to figure out whether to extend GrammarNodeType with structural variants or handle this differently. The user's suggestion to add those structural variants to the enum makes sense, so I'll add Paragraph, Section, Document under a structural banner and use them throughout. Now I'm mapping out the paragraph detection logic following the first path with carry-over handling, then setting up modality detection in the right order across all paths with dual validation checks and a helper function to tie modalities back to their parent nodes.
Writing the OMEX native path... Converting the parsed sentences and grammar tree into GrammarNode objects, verifying that spans are ordered correctly, non-overlapping, and contained within their parents, with each grammar tree rooted at a sentence. If analysis fails on a chunk, I'll fall back to an alternate path. Now I'm rewriting the analyze_text function to implement the full flow with metrics from the graph, then moving into Phase 3 to extract grammar from graphs—fixing bugs where per-sentence extraction adds both grammar_nodes and ChunkGrammarRelationship onto sentences, handling cross-sentence relationships by node_id, and properly managing coreference chains. For chunks that already carry grammar data, I'll skip redundant per-sentence extraction.
The key issue is that node_ids restart per chunk, causing collisions across the corpus. I need to assign globally unique ids in Phase 3 by renumbering all sentence node_ids sequentially before running the pairwise cross-sentence stage, and remapping any parent_node_id references that point to sentences.
Actually, a simpler approach: use a globally increasing id generator during per-chunk processing — I already have SentenceCarryState.next_global_order in path2, so I can extend that to generate unique sentence node_ids via Self::generate_id() (using nanosecond timestamps), which sections and paragraphs already do. This keeps ordering separate: chunk_id plus chunk_offset determines the sequence for sorting in prompts. The cross-sentence stage then uses these globally unique node_ids to map from_sentence_id and to_sentence_id in the response.
For coref chains, I'll store the entire chain on the chunk containing the first mention, avoiding duplication across chunks. The relationship itself lives on the chunk holding the from-sentence, and I need to implement the distribution logic accordingly.
Fixing create_graph next — adding base_node and base_edge helpers plus the keyword move fix, though the function stays structurally the same since TextAnalysisResult now has an empty sections structure and the orchestrator still calls it via CreateGraph with the analysis result.
For basic metrics in analyze_text, I'm computing sentence and paragraph counts directly from the graph nodes after cleaning, then deriving word count from the joined corrected sentence content rather than splitting the cleaned text again.
Now I'm refactoring the DetectModalities handler with a new signature and compile fixes, then removing several helper functions like identify_documents, detect_section_boundary, and the various detect_modalities variants since they're no longer needed. The main stdin stub stays unchanged, but I'm replacing the chunk text test with a synchronous unit test that mocks the cleaning prompt to return "Cleaned text here." and verifies the fallback behavior when JSON extraction fails. Mapping out the orchestrator refactoring — I need to handle the request/preference enum and resolver, update the ProcessedChunk types, fix get_available_pipelines, replace execute_stages and prompt_normalization with cleaner versions, trim gather_methodologies, and then tackle the big piece: the AMT builder with its dispatcher, assembly logic, layer-by-layer construction, and intent-tree bug fixes.
Building build_amt_from_graphs... Still writing union-find logic... Wrapping each pool into an IntentCapture with metadata about parallelism and source tracking, then grouping sentences within pools by their target object or knowledge topic to create branches with summarized verbs, and finally extracting detail-level grammar relationships where negations or modal verbs flag constraints versus regular details. I'm setting up the knowledge paths mapping to track which branches have associated knowledge, then passing that through to the assembly function as an optional parameter that stays empty in zero-shot mode. The assembly function signature now accepts this knowledge map along with the orchestration state and node counter.
Now I'm structuring the tree construction to handle multiple intents by checking if there's more than one capture—if so, each intent gets its own subtree with branches nested underneath, otherwise branches attach directly to the root. I'm factoring out the branch-building logic into a reusable method to avoid duplication across these two cases.
The main orchestration dispatcher checks whether to build the tree from existing file graphs or layer-by-layer based on what's been processed, then validates the result and tracks the validation streak.
For the gather_methodologies step, I'm simplifying it to just record the summary without repeating the classify-build-validate cycle. Then I need to update prompt_normalization to compute the processing path and token budget, passing those into the process_modality function so it can construct the Analyze action with the right context parameters.
I'm also tracking token usage from the LLM response and accumulating it in the state. When keywords haven't been derived yet, I'm traversing the processed chunks to extract subject and object relationships from the grammar nodes.
Now I'm implementing the path resolution function that maps the processing preference to its string representation, with special handling for the Auto variant that checks the model configuration.
For the context limit mapping, I'm considering whether OMEX models should get a higher default since they handle chunking differently than standard context-window-bound models.
I'm restructuring the execute_stages function to follow the correct order: input processing, file handling within stage 2, prompt normalization, and then the modality graph creation steps.
The clarification check needs to stay after the AMT build phase since that's where needs_clarification gets set during simulation, though it could be populated by AMT in the future.
I'm also updating the aggregate_root_modalities function to use the typed OrchModalityDetection, and I need to add the O2 struct block before the ProcessedChunk definitions.
For the test fix, I'll add processing_path to the request literal, and I should check if there are any other references to the grammar_relationships field that was removed from ProcessedChunk in the orchestrator.
Looking at the pipeline output, it now includes sentence_nodes with grammar_relationships and knowledge_refs, so the orchestrator's slim structs need to match those field names exactly — node_id, content, original_content, chunk_id, chunk_offset, paragraph_id, section_id, and the grammar_relationships fields like from_text, to_text, edge_type, tense, negated, verb.
For the verb_type enum that the pipeline serializes as a string like "Action", I'm mapping it to a String in the slim struct with serde defaults to handle optional fields, and since serde ignores unknown fields, I can skip position and nested fields that aren't needed for the pools. The knowledge_refs and cross_sentence_relationships fields mirror the pipeline structure with from_sentence_id, to_sentence_id, relationship_type, and evidence, plus coreference chains with chain_id.
Now I'm ready to write the full implementation — I'll structure it as a short intro paragraph followed by Part 1 covering the modalities/text/main.rs modules with numbered sections and minimal commentary, then Part 2 for the orchestrator/mod.rs. I'm aiming for completeness and will be careful about Rust correctness throughout.
Writing the Rust implementation... So the flow is: detect paragraphs and sentences on the raw text first to anchor spans correctly, then apply whole-chunk cleaning for the corrected text used downstream in keyword/entity/topic extraction, while keeping sentence nodes anchored to the original text for proper byte positioning. For modality and parent relationships in Path1, I'm tying sentences to paragraphs by span containment, then falling back to paragraph containment if needed. Path1 doesn't need the five validation loops that Path2 uses—just a single extraction with deterministic monotonic ordering to keep things consistent. Section tracking continues for the next component. Path1 paragraphs stay prompt-driven without granular validation, keeping it simple. For modality detection in step 3, I'll apply the dual 5x validation universally across both paths. On the state management side, I'm dropping the global order tracking since order restarts per chunk anyway — I'll simplify SentenceCarryState to just track fragment and its absolute position.
Now I'm ready to implement. I'll number the text modifications T1–T24 and orchestrator changes O1–O12, keeping each one concise. For the code structure, I need to make sure GrammarNodeType derives Default properly with the Unknown variant, and map each node type correctly — Section, Paragraph, and Document nodes all get their corresponding enum values.
ChunkGrammarRelationship will use a String for verb_type instead of the full VerbType enum in the orchestrator version. TextModalityOutput needs its Default implementation updated to include the two new fields. The detect_modalities_ordered function takes the chunk text, index, available modalities list, and a mutable carry state, then returns a vector of detections. When carry.open is true, the first prompt targets the end of that open block and produces a detection spanning from 0 to the end with the carried modality.
For continuation handling, I'm storing the local span start at 0 and letting the caller manage absolute positioning, then marking the detection with an intent_reference noting it continues from a previous chunk. I'm extracting the modality detection prompt per the earlier draft and running two validation checks: first confirming whether the span at the given boundaries is the first modality occurrence after a specific offset, then classifying by including the actual text slice. For safety when slicing spans returned by the model, I'm clamping boundaries and using character boundary flooring on both ends.
Writing helper function... I'm realizing the scope here is substantial—fitting all the before/after code examples in a single response will likely exceed the length limit, so I'll need to strategically prioritize and mark continuation points as needed.
